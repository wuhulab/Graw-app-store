# ZeroCat 编程社区
# ${VERSION} 占位符由面板在安装时替换
version: "3"

services:
  zerocat:
    build: .
    image: zerocat:1.0.0
    container_name: zerocat
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    restart: unless-stopped
