# uuWAF
# ${VERSION} 占位符由面板在安装时替换
version: "3"

services:
  uuwaf:
    image: uusec/waf:${VERSION}
    container_name: uuwaf
    volumes:
      - ./data:/opt/waf
      - ./logs:/opt/waf/logs
    ports:
      - "4443:4443"
      - "80:80"
      - "443:443"
    restart: unless-stopped
